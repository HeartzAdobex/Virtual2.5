<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Virtual2._5.MainPage"
             NavigationPage.HasNavigationBar="False">

    <StackLayout BackgroundColor="BurlyWood">

        <Grid x:Name="MainLayout" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <AbsoluteLayout>
                <Image x:Name="BackgroundImage"
                Aspect="Fill"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand"
                AbsoluteLayout.LayoutBounds="0,0,1,1"
                AbsoluteLayout.LayoutFlags="All"/>

                <Slider x:Name="ResizeSlider"
            Minimum="0.5"
            Maximum="3"
            Value="1"
            Grid.Row="1"
            IsVisible="False"
            IsEnabled="True"
            VerticalOptions="CenterAndExpand"
            HorizontalOptions="CenterAndExpand"
            ValueChanged="ResizeSlider_ValueChanged"/>

                <StackLayout Orientation="Horizontal" Grid.Row="2" HorizontalOptions="FillAndExpand">
                    <Button Text="Pick" Clicked="OnPickImageClicked" HorizontalOptions="FillAndExpand" Margin="50,10,10,0"/>
                    <Button Text="Set Background" Clicked="OnSetBackgroundClicked" HorizontalOptions="FillAndExpand" Margin="10,10,10,0"/>
                    <Button Text="Clear" Clicked="OnClearImagesClicked" HorizontalOptions="FillAndExpand" Margin="10,10,10,0"/>
                    <!--<Button Text="Save" Clicked="OnSaveImageClicked" HorizontalOptions="FillAndExpand" />-->
                </StackLayout>
            </AbsoluteLayout>
        </Grid>

    </StackLayout>

</ContentPage>
