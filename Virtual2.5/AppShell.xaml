<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Virtual2._5.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Virtual2._5"
    Shell.FlyoutBehavior="Disabled">

    <FlyoutItem>
        <ShellContent
            Icon="dimension.png"
            Title="2.5D"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        <ShellContent
            Icon="calculation.png"
            Title="Calculation"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        <ShellContent
            Icon="drawing.png"
            Title="Drawing View"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

</Shell>
